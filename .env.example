# AlQuran Backend — Environment Variables
# Copy to .env for local dev, set in Railway dashboard for production.

# ── LLM Provider ─────────────────────────────────────────────────────
LLM_PROVIDER=groq                        # groq | together | openai
GROQ_API_KEY=gsk_your_key_here
TOGETHER_API_KEY=your_together_key_here  # only if LLM_PROVIDER=together
OPENAI_API_KEY=your_openai_key_here      # only if LLM_PROVIDER=openai

# ── Google OAuth (Client Secret not needed — uses ID token flow) ──────
GOOGLE_CLIENT_ID=846223196875-iim6ake76pqe61tufn3t8rccogqv7ec2.apps.googleusercontent.com

# ── JWT ───────────────────────────────────────────────────────────────
JWT_SECRET=generate_with_openssl_rand_hex_32
JWT_EXPIRY_HOURS=72

# ── App URLs ──────────────────────────────────────────────────────────
FRONTEND_URL=https://quranai.vercel.app  # production
# FRONTEND_URL=http://localhost:8080     # local dev

# ── Database ──────────────────────────────────────────────────────────
DATABASE_URL=sqlite:///./quranai.db

# ── Embeddings / Vector Search ────────────────────────────────────────
DATA_DIR=./quran_data
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# ── Optional LLM overrides ────────────────────────────────────────────
# LLM_MODEL=llama-3.3-70b-versatile     # Groq default
# LLM_MODEL=Qwen/Qwen2.5-7B-Instruct-Turbo  # Together default
# LLM_MODEL=gpt-4o-mini                 # OpenAI default
# LLM_TEMPERATURE=0.6
